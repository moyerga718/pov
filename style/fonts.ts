import { Cutive_Mono, Sometype_Mono } from "next/font/google";

export const sometypeMono = Sometype_Mono({ subsets: ["latin"] });
export const cutiveMono = Cutive_Mono({ weight: "400", preload: false });
